
%div{:class => "r28file", :id => "r28file_#{c}", "data-id" => "#{f.id}"}
  .type
    = f.file_ext
  .name
    %div
      = f.file_data
  .data
    = link_to "Платежи", "plategi/#{f.id}"
  .flex
  .download
    = link

